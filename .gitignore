*.xlsx
*.xls
*_scan/
*full_scan/
european_scan/
__pycache__/
.symbols_cache.json
*.pyc
.DS_Store

# transient enrichment artifacts (not for reference)
*.log
industry_cache.json
industry_cache.json.tmp

# raw per-ticker viability DBs (large); only the compact *_summary DBs are committed
viability*.db
!viability_summary*.db
*.db-journal

# SEC EDGAR companyfacts cache (~400MB); rebuilt on demand by pit_fundamentals.py
edgar_cache/
